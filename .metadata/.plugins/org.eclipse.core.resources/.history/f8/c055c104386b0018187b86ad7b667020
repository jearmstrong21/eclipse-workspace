package co.megadodo.lwjglswingshaderfun;

import java.nio.FloatBuffer;

public class BufferUtils {
	
	private BufferUtils() {
		
	}
	
	public static FloatBuffer createFloatBuffer(Vector3f...v3fs) {
		FloatBuffer fb=FloatBuffer.allocate(v3fs.length*3);
		for(Vector3f v:v3fs) {
			fb.put(v.x);
			fb.put(v.y);
			fb.put(v.z);
		}
		return fb;
	}
	
	

}
